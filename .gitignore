# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Stray file some command created via `> nul` redirection in a
# shell that doesn't treat nul as the Windows null device (this
# happens in Git Bash/MSYS). Windows reserves this name, so git
# can't track it — ignore it instead of trying to add/delete it.
nul

# Firebase
.firebase/
.env.firebase
.env.exmple

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
nul
*.sw?
